function App() {
  return (
    <main>
      <h1>Exam</h1>
      {/* TODO: Build your exam UI here.
          - Fetch questions from GET /api/questions
          - Display one question at a time with a progress indicator
          - Allow answer selection, navigate Previous / Next
          - Disable Submit until all questions are answered
          - POST answers to /api/submit and display the score
          See README.md for full requirements and API reference. */}
    </main>
  )
}

export default App
